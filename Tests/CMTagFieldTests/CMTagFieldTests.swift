import XCTest
@testable import CMTagField

final class CMTagFieldTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CMTagField().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
